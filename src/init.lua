local Packages = script.Parent

return {
    FastFourierTransform = require(Packages.FastFourierTransform),
    Graphs = require(Packages.Graphs),
    Interpolation = require(Packages.Interpolation),
    Matrices = require(Packages.Matrices),
    Noise = require(Packages.Noise),
    NumericalMethods = require(Packages.NumericalMethods),
    Ode = require(Packages.Ode),
    PrimeList = require(Packages.PrimeList),
    Primes = require(Packages.Primes),
    Scalars = require(Packages.Scalars),
    Sets = require(Packages.Sets),
    Spline = require(Packages.Spline),
    Tools = require(Packages.Tools)
}